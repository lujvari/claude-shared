#!/usr/bin/env bash
# claude-docker launcher: a.s.r. reference material.
# Container, not a repo; no credentials forwarded.
set -euo pipefail
. "$(dirname "$(readlink -f "$0")")/_lib.sh"
cd_workspace "clients/asr"
exec "$CD_RUN" --claude-auth "$ws" "$@"

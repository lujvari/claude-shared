#!/usr/bin/env bash
# claude-docker launcher: SBP AI (non-CIS) — inat.
# Lives on sbp.gitlab, hence --glab.
set -euo pipefail
. "$(dirname "$(readlink -f "$0")")/_lib.sh"
cd_workspace "sbp/ai/inat"
exec "$CD_RUN" --claude-auth --glab "$ws" "$@"
